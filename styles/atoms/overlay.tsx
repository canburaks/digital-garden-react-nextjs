import { styled } from '../stitches.config';

export const Overlay = styled('div', {
  backgroundColor: 'rgba(23,23,23,0.75)',
  position: 'absolute',
  top: 0,
  left: 0,
  right: 0,
  bottom: 0,
});
